--- a/app/tabs/shop.tsx
+++ b/app/tabs/shop.tsx
@@ -18,7 +18,6 @@
 
-// order email integration
-import { placeOrderEmail } from "../lib/order";
+// order email integration
+import { placeOrderEmail } from "../lib/order";
 
 const applyBuyWithCoins = async (it: Item) => {
     if (it.category === "coins") return; // bundles are cash-only
