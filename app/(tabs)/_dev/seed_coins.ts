// Dev-only coin seeding stub.
// The real seeding already happened in earlier builds.
// This file just exists so `import "./_dev/seed_coins";`
// in app/(tabs)/_layout.tsx doesn't break web bundling.

export {};
