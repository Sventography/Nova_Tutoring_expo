// app/context/AchievementsCoinsBridge.tsx
// Legacy bridge stub.
// Coins + toast rewards for achievements are now handled by AchievementRewardBridge in AppProviders.
// We keep this component as a no-op so existing imports continue to work without errors.

import React from "react";

export default function AchievementsCoinsBridge() {
  return null;
}
