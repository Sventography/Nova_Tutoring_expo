import * as React from "react";

/**
 * Web stub for StripePay — prevents errors when bundling.
 */
export default function StripePayWeb() {
  return (
    <div style={{ padding: 20, color: "#cfe8ef" }}>
      StripePay is not available on web.
    </div>
  );
}
