import * as React from "react";

export default function StripePayFallback() {
  return (
    <div style={{ padding: 20, color: "#cfe8ef" }}>
      StripePay is not available on this platform.
    </div>
  );
}
